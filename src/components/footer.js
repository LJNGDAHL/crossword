import React from "react"

import styles from "./footer.module.css"

const Footer = () => (
  <footer className={styles.fade}>
    <div className={styles.gradient}></div>
  </footer>
)

export default Footer
